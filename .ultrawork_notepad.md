# Ultrawork Notepad — Asthma Agentic RAG + Gradio Deployment
Started: 2026-08-19T00:00:00+02:00

## Plan (exhaustive, atomic)
7-wave implementation from Plan agent (see full plan output):
- Wave 1: scaffold, sources registry, cleaning module, video embed helper, inhaler detector.
- Wave 2: dependencies/uv sync, embeddings, vectorstore/ingest, Groq client, Cohere rerank, system-prompt lock.
- Wave 3: retrieval tool, grader, rewriter, inhaler route node.
- Wave 4: LangGraph StateGraph + pipeline orchestrator.
- Wave 5: Gradio UI + CLI scripts.
- Wave 6: e2e smoke tests + manual QA capture.
- Wave 7: README/AGENTS.md + review-work + handoff.
Scenario contract: (1) happy-path definition question → High confidence + Sources; (2) inhaler query → route=inhaler + embedded ALA video; (3) insufficient evidence → safe fallback; (4) regression lock → system prompt SHA-256 unchanged + scores not treated as evidence.

## Scenarios (the contract) — ALL VERIFIED against real data (2026-08-19)
1. "What is asthma?" → route=retrieve, GINA-2026 p.26 sourced answer, Confidence: High.
2. "How do I use my inhaler?" → route=inhaler, embedded ALA video returned.
3. "What is the capital of France?" → route=retrieve, safe-fallback message.
4. System-prompt SHA-256 lock unchanged (tests/test_prompts.py).

## Now
**ALL WAVES COMPLETE — REVIEW PASSED.** Final 5-lane verdict (2026-08-19): Security PASS; QA PASS (real E2E); Context-mining PASS (inline); consolidated goal+code-quality Oracle PASS/HIGH. Non-blocking recommendations recorded below.

## Non-blocking recommendations (from final review, for future work)
- MAJOR: `agent/inhaler.py` imports `build_youtube_embed` from `ui.video` (agent→UI layering; return video_id from agent instead).
- MINOR: per-query client instantiation (`retrieve()` builds a new ChromaStore; grader/rewriter/generate build new GroqChat per node run).
- MINOR: `INHALER_VIDEO_ID` read via `os.getenv` at import time in `ui/video.py` instead of `Settings` (two sources of truth; `inhaler_video_id` already exists in config).
- MINOR: `@lru_cache` embedding singletons require manual `cache_clear()` in tests.
- NITPICK: batch-size comment in `vectorstore.py` overstates the Chroma limit; 300s Ollama timeout is long.

## Completed
- Wave 1: scaffold, sources registry, cleaning module, video embed helper, inhaler detector.
- Wave 2: dependencies/uv sync, embeddings, vectorstore/ingest, Groq client, Cohere rerank, system-prompt lock.
- Wave 3: retrieval tool, grader, rewriter, inhaler route node.
- Wave 4: LangGraph StateGraph + pipeline orchestrator (`src/asthma_rag/pipeline.py`) + CLI script (`scripts/run_pipeline.py`).
- Wave 5: Gradio UI (`src/asthma_rag/ui/app.py`) + `scripts/ask.py` CLI.
- Wave 6: e2e smoke tests (`tests/test_e2e.py`) covering the scenario contract.
- Wave 7: README refreshed.
- Real-run wave (2026-08-19): .env keys verified; Groq model switched to openai/gpt-oss-120b (llama-3.3-70b retired); package install fixed (hatchling build-system); Ollama GPU embedding backend added (qwen3-embedding:0.6b, 1024-dim, ~36s for 2,257 chunks); real Chroma index built from the 3 PDFs; all 4 scenario contracts verified E2E; Gradio UI smoke-tested (HTTP 200); notebook context-mined (empty-query guard ported; chat-history + RAG-inspection tab noted as future work).

## Todo
- [x] Run full test suite (147 passed)
- [x] Real E2E: index build + scenario contract queries
- [x] UI smoke test (HTTP 200 on :7860)
- [x] Context mining (notebook scan)
- [x] Final review verdict — PASSED (all 5 lanes terminal)

## Findings
- Original notebook: `hackathon (2).py` (1,313 lines), Colab-coupled (`google.colab.userdata`, `/content` paths, `!wget`/`!pip`), hardcodes models (`Qwen/Qwen3-Embedding-0.6B`, `rerank-v4.0-pro`, `command-a-plus-05-2026`), duplicates pipeline 3×, has no threshold logic despite comments, no tests, no src layout.
- Workspace: uv project with only `pymupdf` declared; `.venv` empty; no PDFs locally; precomputed `chunks (2).json` (2,132 chunks from GINA + NHLBI) is reusable.
- Additional authoritative resources (HTTP-verified): WHO 2026 pediatric asthma guideline (CC BY-NC-SA), NICE NG245 (2024), BTS/SIGN SIGN 158, ATS/ERS severe asthma guideline (2014 PDF + 2020 ERJ), Global Asthma Report 2022 (all rights reserved — cite only), NHLBI 2020 focused updates.
- Recommended default inhaler video: American Lung Association “Mastering Your Inhaler: Step-by-Step to Using a Spacer with a Metered-Dose Inhaler” (YouTube ID `TuzCfpeieFA`), embeddable via `gr.HTML` iframe.
- Tech stack (current APIs):
  - Groq: `groq` client, `client.chat.completions.create`, free-tier models incl. `llama-3.3-70b-versatile`; `mixtral-8x7b-32768` deprecated.
  - Cohere: `cohere.ClientV2`, `co.rerank(model="rerank-v4.0-pro", ...)`, map results via `result.index`.
  - Local embedding: `Qwen/Qwen3-Embedding-0.6B` via `SentenceTransformerEmbeddingFunction`; needs `transformers>=4.51.0`, `chromadb>=0.5.0` for kwargs passthrough; ~1.2 GB download, CPU feasible.
  - Gradio video: canonical is `gr.HTML` with YouTube iframe; `gr.Video` does NOT support YouTube watch URLs.
  - Agentic RAG: LangGraph pattern with `generate_query_or_respond → retrieve → grade_documents → generate_answer/rewrite_question`.

## Learnings
- Do not commit copyrighted PDFs; keep a `sources.yaml` with URLs + license notes and a downloader that respects restrictions.
- Centralize config (paths, keys, model names, top-k/threshold) before any pipeline code; this fixes most Colab-to-local migration issues.
- Use `prompt_name="query"` for Qwen3 query embeddings; document embeddings get no prompt.
- Real runs catch what unit tests miss: (1) package was never installed → `python -m` commands failed; (2) `llama-3.3-70b-versatile` is retired on Groq; (3) query-time embeddings must match index-time embeddings or Chroma throws a dimension mismatch; (4) Chroma persists the embedding-function config, so a stale collection conflicts on re-index — delete at client level before rebuilding; (5) real PDFs produce `section_title=None`, which Chroma's metadata rejects — coerce to "" at the vectorstore boundary; (6) Windows cp1252 consoles cannot print Unicode LLM output — reconfigure stdout to UTF-8.
- Ollama GPU (`qwen3-embedding:0.6b` via `/api/embed`) embeds 64 texts in ~1s warm; the full 2,257-chunk index builds in ~36s vs hours on CPU.

---

# Ultrawork Notepad — Iteration 2: Langfuse + verifier/judge LLMs + EXA search route + Groq STT/TTS + Gradio chat overhaul
Started: 2026-08-19T00:00:00+02:00 (iteration 2)

## Plan (exhaustive, atomic)
Gathering context (langfuse skill + 3 librarians) -> Plan agent -> implementation waves -> reviewer gate.

## Scenarios (the contract)
TBD after Plan agent.

## Now
Firing librarian agents (Langfuse SDK, Groq audio APIs, EXA search) + loading langfuse skill + verifying .env keys.

## Todo
- [ ] Gather external context (in progress)
- [ ] Plan agent with full synthesis
- [ ] Implementation waves (TDD)
- [ ] Manual QA on real surface
- [ ] Reviewer gate

## Findings
(append-only)

## Learnings
(append-only)


## Findings (iteration 2)
- .env has LANGFUSE_BASE_URL (not LANGFUSE_HOST) — config must map it for the Python SDK; keys EXA_API_KEY, LANGFUSE_PUBLIC_KEY/SECRET_KEY confirmed present (names only, values unread).
- Packages: langfuse MISSING, exa_py MISSING (need uv add), groq installed (audio.speech endpoint support TBD by librarian).
- Langfuse skill instrumentation reference mandates: framework-first, nested spans w/ correct observation types, session_id for chat, explicit trace input, flush() in CLI scripts, import AFTER dotenv, and a required run-and-self-audit loop (fetch real traces back and audit).
- Audio models on the Groq account confirmed earlier via models.list: whisper-large-v3-turbo, canopylabs/orpheus-v1-english.
- Langfuse: SDK v4 current (4.14.4); use @observe(name, as_type=agent/generation/retriever/evaluator/guardrail), propagate_attributes(session_id, tags, metadata dict[str,str]<=200ch), score_current_trace(name, value, data_type). Missing keys = warning + NoOpTracer no-op (tests safe). LANGFUSE_TRACING_ENABLED=false silences. CLI needs flush(). CRITICAL: pydantic-settings does NOT export .env to os.environ -> pass keys explicitly from Settings to Langfuse(...) constructor.
- Groq audio: transcriptions.create(file=Path, model='whisper-large-v3-turbo', language) -> .text; speech.create(model='canopylabs/orpheus-v1-english', voice in {autumn,diana,hannah,austin,daniel,troy}, input<=200 chars, response_format='wav' ONLY) -> .content bytes. Vocal directions [cheerful]/[whisper]. Rate: whisper 20RPM/2KRPD, orpheus 10RPM/100RPD. Gradio: mic input type='filepath'; TTS output = temp .wav filepath.
- EXA: exa-py; Exa(api_key=...) explicit (same os.environ caveat); search() returns text by default, search_and_contents() DEPRECATED; freshness: contents={'max_age_hours':24,'text':{'max_characters':N}} + start_published_date ISO8601 + user_location='EG'; videos: include_domains=['youtube.com/watch'], extract ID via regex (?:v=|youtu\.be/|shorts/)([\w-]{{11}}); 10 QPS, ~/1k searches.
- Design decisions: verifier node = openai/gpt-oss-20b (fast classify), judge node = openai/gpt-oss-120b (nuanced safety), both configurable via Settings; judge runs after generate_answer + search answers (safe_fallback bypasses); TTS truncates to <=200 chars at sentence boundary; UI converts to gr.Chatbot (fixes duplicate loading + scrolling), mic/speak buttons, custom theme.

## Plan (from Plan agent, locked 2026-08-19)
Waves: A(foundation: deps/config/observability) -> B+C+D parallel(verifier/judge/websearch+search-nodes/audio) -> E(state->graph rewire->test updates+branch tests) -> F(pipeline observe) -> G(UI chat rewrite + ask.py + UI tests) -> H(real QA: langfuse trace fetch, STT roundtrip, EXA smoke, visual QA, full suite, review-work). Success criteria: >=160 tests green; ask.py prints route+confidence; out-of-scope refusal; EXA-sourced price answer; video_search embed; UI chat scrolls, single spinner, mic+speak buttons; langfuse trace with verifier+judge scores; SYSTEM_PROMPT hash unchanged. Plan agent session: ses_fe4e5daebffeqcR89b42udqkKC (continuation for refinements).

## Scenarios (the contract, iteration 2)
- S1 happy path: 'What is asthma?' -> sourced answer + [route: retrieve] + [confidence: High] + langfuse trace w/ verifier+judge scores
- S2 out-of-scope: 'capital of France' -> verifier refusal message, RAG skipped, [refused: out_of_scope]
- S3 dangerous answer: judge refuse -> refusal message + refusal_reason logged
- S4 search route: 'ventolin price in Egypt' -> EXA-sourced answer + [route: search]
- S5 video route: 'how to use a nebulizer' -> embedded video + [route: video_search]; 'how do I use my inhaler' -> still inhaler route
- S6 audio: STT transcribes fixture wav; TTS speaks <=200 char truncated answer
- S7 UI: chat scrolls, single spinner, mic+speak buttons, video in chat
- S8 regression: all 4 iteration-1 e2e scenarios + SYSTEM_PROMPT hash unchanged

## Now (execution)
Wave A0 complete -> A1 config RED->GREEN -> A2 observability RED->GREEN.
